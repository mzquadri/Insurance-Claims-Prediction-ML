# What the reproducible path needs: the benchmark, the tests and the figures.
# Pinned to the versions the recorded results in results/benchmark.json were
# produced with. The optional extras below are not needed for any of that.

numpy==2.4.3
pandas==2.3.3
scikit-learn==1.8.0
scipy==1.17.0
matplotlib==3.10.8
joblib==1.5.3
